<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kosmo.orange.wtf.model.mapper.MemberMapper">

    <insert id="signUp" >
        INSERT INTO member(email,PASSWORD,nickname,birth,gender,regist_date,favor)
         VALUES(#{email},#{password},#{nickname},#{birthday},#{gender},SYSDATE(),#{favor});
    </insert>

    <select id="memberLogin" resultMap="getMember">
        SELECT * FROM member
        WHERE  email = #{email}
    </select>

    <select id="idcheckMember"  resultMap="getMember">
        SELECT * FROM member
        WHERE  email = #{email};
    </select>
    <update id="updateUserPassword">
        update member
        set password = #{password}
        WHERE email =#{email};


    </update>

    <update id="memberUpdate" >
        update  member SET birth=#{birthday}, gender=#{gender}, nickname =#{nickname}
        WHERE email = #{email};
    </update>

    <update id="passwordChge">
        update member SET password=#{password}
        WHERE email = #{email};

    </update>

    <resultMap id="getMember" type="MemberVO">
        <result property="memberId" column="member_id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="birthday" column="birth"/>
        <result property="gender" column="gender"/>
        <result property="favor" column="favor"/>
        <result property="point" column="point"/>
        <result property="registDate" column="regist_date"/>
        <result property="exitCheck" column="exit_check"/>
        <result property="exitDate" column="exit_date"/>

    </resultMap>

</mapper>




