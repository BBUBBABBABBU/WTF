--변경 부분
--alter table restaurant add column like_count int;

    member 테이블 favor 컬럼 추가
-- ALTER TABLE member ADD favor VARCHAR(600);

    ChatRoom 테이블 생성
-- CREATE TABLE chatroom(
    	room_id int AUTO_INCREMENT PRIMARY KEY);

    chatMessage 테이블 생성
-- CREATE TABLE chatMessage(
      	message_id INT AUTO_INCREMENT PRIMARY KEY,
      	room_id INT,
      	member_id INT,
      	message TEXT,
      	TIME DATE,
      	FOREIGN KEY(room_id) REFERENCES chatRoom(room_id),
      	FOREIGN KEY(member_id) REFERENCES member(member_id));

    chatRoomJoin 테이블 생성
-- CREATE TABLE chatroomjoin(
   	join_id INT AUTO_INCREMENT PRIMARY KEY,
   	member_id INT,
   	room_id INT,
   	FOREIGN KEY(member_id) REFERENCES member(member_id),
   	FOREIGN KEY(room_id) REFERENCES chatroom(room_id));

    Review_test 테이블 생성
-- CREATE TABLE Review_test(
   	review_id INT AUTO_INCREMENT PRIMARY KEY,
   	member_id INT,
   	res_id INT,
   	rating_avg FLOAT NOT null,
   	recommend_count INT,
   	charge_count INT);


-- INSERT INTO review_test (member_id,res_id,rating_avg,recommend_count)
  VALUES (truncate((1+RAND()*20),0),truncate((1+RAND()*151),0),truncate((1+RAND()*5),0),truncate((1+RAND()*300),0));